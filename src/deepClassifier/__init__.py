import os
import sys
import logging

logging_str = "[%(asctime)s: %(levelname)s: %(module)s]: %(messag
        # logging.StreamHandler(sys.stdout),
    ])

logger = logging.getLogger("deepClassifierLogger")